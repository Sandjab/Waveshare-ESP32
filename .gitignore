# PlatformIO
.pio/
.vscode/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Python
__pycache__/
*.pyc

# Inbox (temporary staging, not tracked)
inbox/

# Claude Code permissions (per-user / per-OS)
.claude/settings.local.json

# Personal device inventory (MAC addresses, per-user)
devices.local.yaml

# WiFi credentials (per-user, never committed) — see *.example siblings
**/secrets.h
